# Code Chronicle — admin_requests_page.dart

## 📂 File Registry
* **Workspace Path**: `app/lib/features/admin/presentation/admin_requests_page.dart`
* **Type**: Presentation Page

## 📖 Operational Purpose
Defines the implementation and structures for admin_requests_page.dart.

---

## ⛓️ Import Dependencies
* `package:flutter/material.dart`
* `package:flutter/services.dart`
* `package:flutter_riverpod/flutter_riverpod.dart`
* `package:uuid/uuid.dart`
* `../../../app/theme/tokens.dart`
* `../../auth/domain/auth_service.dart`
* `../../auth/domain/auth_state.dart`
* `../../../shared/domain/enums.dart`
* `../../../shared/presentation/widgets/neon_plate.dart`
* `../../../shared/util/url_helper.dart`

---

## 🏛️ Declared Classes
### `class AdminRequestsPage`
* Represents a structured code construct mapping business logic or view layouts for admin_requests_page.dart.

## ⚙️ Methods & Routines (CQS Boundaries)
* **`SizedBox()`**: Handles data retrieval queries (Query).
* **`_reject()`**: Handles state mutation commands (Command).
* **`_showInviteDialog()`**: Handles data retrieval queries (Query).
* **`Uuid()`**: Handles data retrieval queries (Query).
* **`AdminRequestsPage()`**: Handles data retrieval queries (Query).
* **`TextStyle()`**: Handles data retrieval queries (Query).
* **`Text()`**: Handles data retrieval queries (Query).
* **`Icon()`**: Handles data retrieval queries (Query).
* **`createState()`**: Handles state mutation commands (Command).
* **`_approve()`**: Handles state mutation commands (Command).
* **`Center()`**: Handles data retrieval queries (Query).
* **`_copyLink()`**: Handles data retrieval queries (Query).
* **`build()`**: Handles data retrieval queries (Query).

---

## 🛡️ Verification Tiers
* **Static Analysis**: Verified via `flutter analyze` linter constraints.
* **Coverage**: Audited against unit and widget test pipelines.
