# Code Chronicle — documents_list_page.dart

## 📂 File Registry
* **Workspace Path**: `app/lib/features/documents/presentation/documents_list_page.dart`
* **Type**: Presentation Page

## 📖 Operational Purpose
Icon per document type/title Quick filter chips: All / ID Scan / Consent Form / Work Pic

---

## ⛓️ Import Dependencies
* `package:infernal_ink_steel/shared/data/org_labels_provider.dart`
* `package:flutter/material.dart`
* `package:flutter_riverpod/flutter_riverpod.dart`
* `package:go_router/go_router.dart`
* `../../../app/theme/tokens.dart`
* `../data/documents_provider.dart`
* `../../../../shared/domain/document.dart`
* `../../../shared/data/infernal_labels_provider.dart`

---

## 🏛️ Declared Classes
### `class DocumentsListPage`
* Represents a structured code construct mapping business logic or view layouts for documents_list_page.dart.

### `class _TypeFilterChips`
* Represents a structured code construct mapping business logic or view layouts for documents_list_page.dart.

## ⚙️ Methods & Routines (CQS Boundaries)
* **`SizedBox()`**: Handles data retrieval queries (Query).
* **`_TypeFilterChips()`**: Handles data retrieval queries (Query).
* **`TextStyle()`**: Handles data retrieval queries (Query).
* **`_iconForDoc()`**: Handles data retrieval queries (Query).
* **`_shortenPath()`**: Handles data retrieval queries (Query).
* **`Icon()`**: Handles data retrieval queries (Query).
* **`createState()`**: Handles state mutation commands (Command).
* **`Center()`**: Handles data retrieval queries (Query).
* **`DocumentsListPage()`**: Handles data retrieval queries (Query).
* **`build()`**: Handles data retrieval queries (Query).
* **`_colorForDoc()`**: Handles data retrieval queries (Query).

---

## 🛡️ Verification Tiers
* **Static Analysis**: Verified via `flutter analyze` linter constraints.
* **Coverage**: Audited against unit and widget test pipelines.
