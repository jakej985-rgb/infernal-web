# Code Chronicle — integrations_page.dart

## 📂 File Registry
* **Workspace Path**: `app/lib/features/settings/presentation/integrations_page.dart`
* **Type**: Presentation Page

## 📖 Operational Purpose
Defines the implementation and structures for integrations_page.dart.

---

## ⛓️ Import Dependencies
* `package:flutter/material.dart`
* `package:flutter_riverpod/flutter_riverpod.dart`
* `../../../app/theme/tokens.dart`
* `../../../shared/data/org_provider.dart`
* `../../../shared/core/services/integration_service.dart`
* `../../../shared/presentation/widgets/neon_divider.dart`
* `../../../shared/presentation/widgets/neon_plate.dart`
* `../../../shared/util/url_launcher_helper.dart`

---

## 🏛️ Declared Classes
### `class IntegrationsPage`
* Represents a structured code construct mapping business logic or view layouts for integrations_page.dart.

## ⚙️ Methods & Routines (CQS Boundaries)
* **`_buildGateways()`**: Handles data retrieval queries (Query).
* **`createState()`**: Handles state mutation commands (Command).
* **`_forceSyncCalendar()`**: Handles data retrieval queries (Query).
* **`SizedBox()`**: Handles data retrieval queries (Query).
* **`_saveSmtpConfig()`**: Handles state mutation commands (Command).
* **`_disconnect()`**: Handles data retrieval queries (Query).
* **`Text()`**: Handles data retrieval queries (Query).
* **`Divider()`**: Handles data retrieval queries (Query).
* **`IntegrationsPage()`**: Handles data retrieval queries (Query).
* **`_connectGoogle()`**: Handles data retrieval queries (Query).
* **`dispose()`**: Handles data retrieval queries (Query).
* **`_testSmtpConnection()`**: Handles data retrieval queries (Query).
* **`_buildGatewayInfo()`**: Handles data retrieval queries (Query).
* **`BorderSide()`**: Handles data retrieval queries (Query).
* **`_buildSmtpForm()`**: Handles data retrieval queries (Query).
* *And 4 additional helper routine(s) mapping internal operations.*

---

## 🛡️ Verification Tiers
* **Static Analysis**: Verified via `flutter analyze` linter constraints.
* **Coverage**: Audited against unit and widget test pipelines.
