# Code Chronicle — stats_overview_page.dart

## 📂 File Registry
* **Workspace Path**: `app/lib/features/reports/presentation/stats_overview_page.dart`
* **Type**: Presentation Page

## 📖 Operational Purpose
Defines the implementation and structures for stats_overview_page.dart.

---

## ⛓️ Import Dependencies
* `package:infernal_ink_steel/shared/data/org_labels_provider.dart`
* `package:flutter/material.dart`
* `package:flutter_riverpod/flutter_riverpod.dart`
* `package:intl/intl.dart`
* `../../../app/theme/tokens.dart`
* `../data/stats_provider.dart`
* `../../../shared/data/infernal_labels_provider.dart`

---

## 🏛️ Declared Classes
### `class StatsOverviewPage`
* Represents a structured code construct mapping business logic or view layouts for stats_overview_page.dart.

### `class _MetricCard`
* Represents a structured code construct mapping business logic or view layouts for stats_overview_page.dart.

## ⚙️ Methods & Routines (CQS Boundaries)
* **`SizedBox()`**: Handles data retrieval queries (Query).
* **`_buildBody()`**: Handles data retrieval queries (Query).
* **`_buildRevenueChart()`**: Handles data retrieval queries (Query).
* **`_getStatusColor()`**: Handles data retrieval queries (Query).
* **`TextStyle()`**: Handles data retrieval queries (Query).
* **`Offset()`**: Handles state mutation commands (Command).
* **`StatsOverviewPage()`**: Handles data retrieval queries (Query).
* **`_buildMetricGrid()`**: Handles data retrieval queries (Query).
* **`Icon()`**: Handles data retrieval queries (Query).
* **`_buildStatusSection()`**: Handles data retrieval queries (Query).
* **`Center()`**: Handles data retrieval queries (Query).
* **`build()`**: Handles data retrieval queries (Query).
* **`takeLast()`**: Handles data retrieval queries (Query).

---

## 🛡️ Verification Tiers
* **Static Analysis**: Verified via `flutter analyze` linter constraints.
* **Coverage**: Audited against unit and widget test pipelines.
