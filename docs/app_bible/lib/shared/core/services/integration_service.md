# Code Chronicle — integration_service.dart

## 📂 File Registry
* **Workspace Path**: `app/lib/shared/core/services/integration_service.dart`
* **Type**: Data Model / Utility

## 📖 Operational Purpose
Defines the implementation and structures for integration_service.dart.

---

## ⛓️ Import Dependencies
* `dart:async`
* `package:riverpod_annotation/riverpod_annotation.dart`
* `../../data/org_provider.dart`

---

## 🏛️ Declared Classes
### `class GoogleIntegrationStatus`
* Represents a structured code construct mapping business logic or view layouts for integration_service.dart.

### `class SmtpIntegrationStatus`
* Represents a structured code construct mapping business logic or view layouts for integration_service.dart.

### `class IntegrationConfig`
* Represents a structured code construct mapping business logic or view layouts for integration_service.dart.

### `class GoogleContact`
* Represents a structured code construct mapping business logic or view layouts for integration_service.dart.

### `class IntegrationService`
* Represents a structured code construct mapping business logic or view layouts for integration_service.dart.

## ⚡ State Providers & Notifiers
* **Provider Key**: `watchIntegrationConfig` (Riverpod container reactive handle)
* **Provider Key**: `integrationService` (Riverpod container reactive handle)

## ⚙️ Methods & Routines (CQS Boundaries)
* **`Exception()`**: Handles data retrieval queries (Query).
* **`IntegrationService()`**: Handles data retrieval queries (Query).
* **`watchConfig()`**: Handles data retrieval queries (Query).
* **`integrationService()`**: Handles data retrieval queries (Query).
* **`disconnectIntegration()`**: Handles data retrieval queries (Query).
* **`watchIntegrationConfig()`**: Handles data retrieval queries (Query).

---

## 🛡️ Verification Tiers
* **Static Analysis**: Verified via `flutter analyze` linter constraints.
* **Coverage**: Audited against unit and widget test pipelines.
