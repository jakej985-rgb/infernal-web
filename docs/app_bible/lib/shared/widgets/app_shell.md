# Code Chronicle — app_shell.dart

## 📂 File Registry
* **Workspace Path**: `app/lib/shared/widgets/app_shell.dart`
* **Type**: Data Model / Utility

## 📖 Operational Purpose
Application shell with navigation (sidebar/bottom nav) Navigation item configuration Dynamic list of navigation items Main app shell that provides consistent navigation

---

## ⛓️ Import Dependencies
* `package:infernal_ink_steel/shared/data/org_labels_provider.dart`
* `package:flutter/material.dart`
* `package:flutter_riverpod/flutter_riverpod.dart`
* `package:go_router/go_router.dart`
* `../../app/router.dart`
* `../../app/theme/tokens.dart`
* `../data/infernal_labels_provider.dart`

---

## 🏛️ Declared Classes
### `class NavItem`
* Represents a structured code construct mapping business logic or view layouts for app_shell.dart.

### `class AppShell`
* Represents a structured code construct mapping business logic or view layouts for app_shell.dart.

### `class _AppDrawer`
* Represents a structured code construct mapping business logic or view layouts for app_shell.dart.

### `class _NavigationRail`
* Represents a structured code construct mapping business logic or view layouts for app_shell.dart.

## ⚙️ Methods & Routines (CQS Boundaries)
* **`SizedBox()`**: Handles data retrieval queries (Query).
* **`VerticalDivider()`**: Handles data retrieval queries (Query).
* **`_selectedIndex()`**: Handles data retrieval queries (Query).
* **`_navigate()`**: Handles data retrieval queries (Query).
* **`AppShell()`**: Handles data retrieval queries (Query).
* **`_AppDrawer()`**: Handles data retrieval queries (Query).
* **`_NavigationRail()`**: Handles data retrieval queries (Query).
* **`build()`**: Handles data retrieval queries (Query).

---

## 🛡️ Verification Tiers
* **Static Analysis**: Verified via `flutter analyze` linter constraints.
* **Coverage**: Audited against unit and widget test pipelines.
